 @model SiteX.Web.ViewModels.BlogViewModels.BlogToSelectedList;
@{

}

<div id="Categories" class="card">
    <p>Genres: </p>
    @foreach (var genre in Model.GenresToList)
    {
        <a title="item" asp-action="SearchByGenre" asp-route-id="@genre.Id" class="btn btn-primary ">@genre.Name</a>
    }
</div>
